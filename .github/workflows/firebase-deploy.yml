# .github/workflows/firebase-deploy.yml

name: Deploy a NCLEX by Rey

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy_hosting:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Descargar Código
        uses: actions/checkout@v4

      - name: 🚀 Subir a Firebase
        # ✅ SOLUCIÓN: Fijar a la versión estable v15.5.0 para evitar el bug de la v15.6.0
        uses: w9jds/firebase-action@v15.5.0
        with:
          args: deploy --only hosting
        env:
          # Aquí usamos la llave NUEVA que guardaste en Secrets
          GCP_SA_KEY: ${{ secrets.NUEVO_FIREBASE_KEY }}
          # Y aquí el nombre de tu proyecto NUEVO
          PROJECT_ID: nclexbyrey