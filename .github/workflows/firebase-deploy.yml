name: Deploy Blindado (Solo Hosting)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy_hosting:
    runs-on: ubuntu-latest
    steps:
      # 1. Descargar tus archivos del repositorio
      - name: 📥 Descargar Código
        uses: actions/checkout@v4

      # 2. Subir a Firebase usando Docker (Aislado y Seguro)
      # Usamos w9jds/firebase-action que es el estándar militar.
      # No intenta compilar nada, solo sube los archivos.
      - name: 🚀 Subir a Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting
        env:
          GCP_SA_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_NCLEX_LIBRARY_9E9E7 }}
          PROJECT_ID: nclex-library-9e9e7
