name: Deploy Blindado (Solo Hosting)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy_hosting:
    runs-on: ubuntu-latest
    steps:
      # 1. Descargar tus archivos del repositorio
      - name: 📥 Descargar Código
        uses: actions/checkout@v4

      # 2. PASO DE VERIFICACIÓN (Rayos X)
      # Esto nos mostrará en el log si la carpeta 'public' tiene archivos o está vacía.
      - name: 🔍 Diagnóstico de Archivos
        run: |
          echo "--- ¿Qué hay en la raíz? ---"
          ls -la
          echo "--- ¿Qué hay dentro de public? ---"
          ls -la public || echo "⚠️ ¡CUIDADO! La carpeta public no existe aquí."

      # 3. Subir a Firebase
      - name: 🚀 Subir a Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting
        env:
          GCP_SA_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_NCLEX_LIBRARY_9E9E7 }}
          PROJECT_ID: nclex-library-9e9e7
